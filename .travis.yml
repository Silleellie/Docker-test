env:
  global:
    - CI_NAME:travis-pro

sudo: required

language: python

python:
  - "3.8"

cache: pip

services:
  - docker

jobs:
  include:
    - stage: build docker image
      script:
      - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
      - docker build -t prova Dockerfile
      - docker images
      - docker tag prova $DOCKER_USERNAME/prova
      - docker push $DOCKER_USERNAME/prova
    - stage: test
      script:
      - docker run --rm $DOCKER_USERNAME/prova cat python print("we")
  
git:
  depth: false
